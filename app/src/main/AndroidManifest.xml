<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Erlaubnis für Bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/bier_icon_and_roundicon_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/bier_icon_and_roundicon_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.KönigDerSchlückeOhneBox"
        tools:targetApi="31">

        <activity
            android:name=".view.StartbildschirmActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.spiel.HauptspielActivity"></activity>

        <activity android:name=".view.SpielBeitretenActivity"></activity>

        <activity android:name=".view.StatistikActivity"></activity>

        <activity android:name=".view.EinstellungenActivity"></activity>

        <activity android:name=".view.FeedbackActivity"></activity>

        <activity android:name=".view.lobby.LobbyActivity"></activity>

        <activity android:name=".view.regelsets.RuleManagerActivity"></activity>

    </application>

</manifest>